export const initialState = {
  dataUser: [],
};

// ==============================|| CUSTOMIZATION REDUCER ||============================== //

const user = (state = initialState, action: any) => {
  let id;
};

export default user;
